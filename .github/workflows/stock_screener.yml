name: Stock Deviation Screener

on:
  # 毎営業日 16:30 JST = 07:30 UTC（東京市場クローズ後、少し余裕を持たせる）
  schedule:
    - cron: '30 7 * * 1-5'
  # 手動実行
  workflow_dispatch:

jobs:
  screener:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Japanese font
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk
          fc-cache -f

      - name: Run stock screener
        run: |
          mkdir -p output
          OUTPUT_DIR=output python stock_deviation_screener.py

      - name: Upload Excel as artifact
        uses: actions/upload-artifact@v4
        with:
          name: stock-deviation-excel
          path: output/stock_deviation_*.xlsx
          retention-days: 30

      - name: Commit & Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/
          git diff --cached --quiet || git commit -m "📊 Stock screener $(date +%Y-%m-%d)"
          git push
